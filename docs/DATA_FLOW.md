# Literature Data Flow

Search configuration
→ Search engine
→ Source provider
→ Raw article response
→ Normalizer
→ Hits queue
→ Screening engine
→ Intake engine
→ QC engine
→ Reports
→ Audit trail

## Rule

Raw external data must never directly enter workflow pages.

All external data must be normalized first.
