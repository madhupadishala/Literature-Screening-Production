# Nexus RC1 — UAT Sprints 4–6 Status

## Scope
This record covers release-preparation Sprints 4–6:
1. isolated UAT runtime/environment;
2. Nexus database migrations 022–031 and schema verification;
3. end-to-end / negative / tenant-isolation UAT readiness and evidence.

## Sprint 4 — Isolated UAT environment

### Completed
- RC1 preview deployments are generated from `release/nexus-integrated-rc1`.
- Latest verified preview lineage is non-production and Vercel-protected.
- A standalone Next.js RC1 runtime artifact is generated by GitHub Actions.
- An isolated Neon branch exists:
  - project: `clinixai-validation-db`
  - branch name: `nexus-uat-rc1-20260922`
  - database: `literature_screening_prod`
- Production database/schema has not been modified by this UAT activity.
- A preview-only DB fingerprint endpoint is implemented.
- The fingerprint endpoint now fails closed unless:
  - Vercel environment is `preview`;
  - Nexus environment is explicitly `UAT`;
  - migration head is `031`;
  - all ten Nexus migrations 022–031 are recorded;
  - at least 31 safety tables are present;
  - both controlled UAT tenants exist;
  - Neon project and branch identity are available.
- `UAT-AUTO-007` makes this database-binding fingerprint a mandatory automated UAT scenario.

### Open platform binding
The currently deployed preview has been observed reaching a database where
`clinixai_schema_migrations` is absent. Therefore the Vercel preview's
`DATABASE_URL` / Nexus environment binding does not yet point at the isolated
UAT Neon branch.

This is intentionally treated as a BLOCK condition, not waived.

## Sprint 5 — Migrations 022–031

**Status: COMPLETE**

Verified on isolated Neon branch `nexus-uat-rc1-20260922`:

- total migration ledger entries: 31;
- Nexus migration entries: 10;
- first Nexus migration: 022;
- last Nexus migration: 031;
- all Nexus checksums are 64-character hashes;
- all ten Nexus migration checksums are distinct;
- safety_* table count: 31;
- UAT module entitlements are present;
- platform role assignment exists;
- Case Processing is enabled only for controlled Tenant A alongside Intake;
- Tenant B has Literature enabled but Intake and Case Processing disabled.

Database immutability controls verified:
- case drafts;
- case assessments;
- narrative versions;
- review actions;
- finalization checks;
- evidence packages;
- exports.

## Controlled UAT tenant matrix

### Tenant A — nexus-uat-rc1-a
UAT entitlements:
- Literature: enabled
- Intake: enabled
- Case Processing: enabled

Synthetic UAT roles:
- PV Administrator / processor
- QC Reviewer
- Medical Reviewer
- Quality Approver
- Read Only

### Tenant B — nexus-uat-rc1-b
UAT entitlements:
- Literature: enabled
- Intake: disabled
- Case Processing: disabled

Synthetic UAT role:
- Read Only

This tenant is the negative-control tenant for direct-route/module/tenant-isolation tests.

## Sprint 6 — E2E and negative testing

### Passed evidence
At the RC1 code/release gate:
- Nexus Sprints 1–10 verification passed;
- eight regulated golden-case scenarios passed;
- module and direct API guard assertions passed;
- Case Processing entitlement dependency on Intake passed;
- duplicate cannot create a new case;
- follow-up routes to existing case;
- missing reporter/incomplete case is blocked;
- multi-product/multi-event assessment completeness is enforced;
- QC return/open query blocks finalization;
- current-revision QC + Medical Review approvals are required;
- finalization creates immutable case version;
- evidence/export boundaries are verified;
- raw source bytes are excluded from evidence JSON;
- dependency security audit is zero-vulnerability;
- optimized production Next.js build passes.

### Database controls passed on actual UAT branch
- migration/schema integrity;
- entitlement split between Tenant A and Tenant B;
- immutable-history triggers;
- foreign-key/unique constraints required by Nexus;
- isolated synthetic UAT fixtures with no safety-case data pollution at baseline.

### Deployment-backed execution — BLOCKED
A full deployed UI/API → isolated UAT database walkthrough cannot be signed off
until the Vercel preview is bound to the isolated UAT Neon branch and
`UAT-AUTO-007` returns HTTP 200.

The connected Vercel tooling available to this release session can inspect
projects/deployments/logs but cannot mutate preview environment variables.
Credentials are not committed to source control and deployment protection is
not weakened.

## Exit gate
Sprints 4 and 6 become fully PASS only when the preview deployment is configured
with the UAT database connection and `NEXUS_DEFAULT_ENVIRONMENT=UAT`, followed
by:
1. `UAT-AUTO-007` = PASS;
2. automated UAT probes = PASS;
3. Tenant A happy-path synthetic case execution = PASS;
4. Tenant B module/direct-route negative tests = PASS;
5. cross-tenant record access negative tests = PASS;
6. final evidence attached to the release record.

No production release approval is implied by this document.
